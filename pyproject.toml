[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.hf.metadata]
sdk = "docker"
app_file = "app.py"
title = "email-env-openenv"
emoji = "🤖"
colorFrom = "blue"
colorTo = "green"
pinned = false